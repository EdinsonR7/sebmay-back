<%- layout('layouts/main') %>

  <a href="/productos/new" class="btn">➕ Nuevo Producto</a>
  <hr />

  <table border="1" cellpadding="6" cellspacing="0">
    <thead>
      <tr>
        <th>ID</th>
        <th>Nombre</th>
        <th>Categoría</th>
        <th>Tipo</th>
        <th>Precio</th>
        <th>Stock</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% productos.forEach(p=> { %>
        <tr>
          <td>
            <%= p.id %>
          </td>
          <td><a href="/productos/<%= p.id %>">
              <%= p.nombre %>
            </a></td>
          <td>
            <%= p.categoria %>
          </td>
          <td>
            <%= p.tipo %>
          </td>
          <td>
            <%= p.precio %>
          </td>
          <td>
            <%= p.stock %>
          </td>
          <td>
            <a href="/productos/<%= p.id %>/edit" class="btn-edit">✏️ Editar</a>
            <form action="/productos/<%= p.id %>?_method=DELETE" method="POST" style="display:inline;">
              <button type="submit" class="btn-delete" onclick="return confirm('¿Eliminar este producto?')">🗑️
                Eliminar</button>
            </form>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table>
  </body>

  </html>